#include "Actor.h"

namespace UT
{
    Actor::Actor()
        : position({ 0, 0 }), sprite(nullptr), collisionBox({0, 0, 0, 0}), speed(0)
    {

    }

    void Actor::MoveTo(sf::Vector2i position)
    {

    }
}