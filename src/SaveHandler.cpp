#include "SaveHandler.h"

namespace UT
{
    void SaveHandler::SaveData(std::string filename, std::map<std::string, Datatype> dataype, FileEncryption encryption)
    {

    }

    std::map<std::string, Datatype> SaveHandler::LoadData(std::string filename, FileEncryption encryption)
    {

    }
}